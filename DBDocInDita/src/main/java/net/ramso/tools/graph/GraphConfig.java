package net.ramso.tools.graph;

public class GraphConfig {
	public static void loadIcons() {
		// URL url =
		// Thread.currentThread().getContextClassLoader().getResource(GraphConstants.SHAPES_FOLDER);
		// File dir;
		// try {
		// dir = new File(url.toURI());
		// for (File file : dir.listFiles()) {
		//
		// }
		// } catch (URISyntaxException e) {
		// // TODO Auto-generated catch block
		// e.printStackTrace();
		// }

	}

	public static void loadShapes() {
		// File files = new File(
		// Thread.currentThread().getContextClassLoader().getResource(GraphConstants.SHAPES_FOLDER).getPath());
		//
		// for (File file : files.listFiles()) {
		// Document doc;
		// try {
		// doc = FileTools.parseXML(file);
		// mxGraphics2DCanvas.putShape(file.getName().substring(0,
		// file.getName().indexOf(".") - 1).toLowerCase(),
		// new mxStencilShape(doc));
		// } catch (ParserConfigurationException | SAXException | IOException e) {
		// LogManager.warn("No se ha añadido el Shape " + file.getName(), e);
		// }
		//
		// }

	}

}
